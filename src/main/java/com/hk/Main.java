package com.hk;

public class Main {

    public static void main(String[] args) throws InterruptedException {

        // THREAD SECTION

        /*
       for (int i=0; i<5;i++){
         //   ThreadObject threadObject = new ThreadObject(i);
         //   threadObject.start(); // Thread'lerde sıralama yok, birbirinden ayrık oldukları için random hareket ederler.
           ThreadUsingRunnable myThing = new ThreadUsingRunnable(i);
           Thread thread = new Thread(myThing);
           thread.start();
          // thread.join(); // .run() gibi çalışır; diğer threadi bekler.
        }

        // MULTI THREADING

        // metottaki run ifadesini çağırırsak ayrı thread olarak çalıştırmayıp bitmesini bekleyecek.
        // threadObject.run();
        // threadObject2.run();

        // Start metoduyla threadleri ayrı ayrı başlatabiliriz. Threadlerden birinde hata aldığımız zaman diğer threadler çalışmaya devam eder.
        //threadObject.start();
        //threadObject2.start();

        //
         */

       // END OF THREAD SECTION

    }
}